# --- large model artifacts / caches: NEVER commit (GitHub rejects >100MB) ---
# Share these via Google Drive or a GitHub Release instead.
*.zip
*.msgpack
model_data/*.zip
jax_cache/
**/jax_cache/
**/models/

# --- cloned upstream repos (regenerated by the pipeline cells; not our work to commit) ---
Mava/
InforMARLcodebase/

# --- python / jupyter ---
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# --- os / editor cruft ---
.DS_Store
**/.DS_Store

# NOTE: we DO keep the small results — model_data/*.csv, figures/*.png, analysis/**, curves JSONs.
